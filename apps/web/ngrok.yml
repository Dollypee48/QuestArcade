# Ngrok configuration file
# This file is used when running: ngrok start --config=ngrok.yml
# Or set NGROK_CONFIG environment variable to point to this file

version: "2"
authtoken: 36DGaCXsMl7RJ9vzU7Dwv7ni6lz_mBpSkvjpJjVpQzndMrr
region: us

tunnels:
  questarcade:
    addr: 3000
    proto: http
    inspect: true
    bind_tls: true
    # Request headers
    request_header:
      add:
        - "X-Forwarded-Proto: https"
        - "X-Forwarded-For: $remote_addr"
    # Compression
    compression: true

